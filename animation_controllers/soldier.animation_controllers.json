{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.soldier": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": ["idle"],
          "transitions": [
            { "attack": "query.property('siedler:combat_state') == 'attack'" },
            { "move": "query.property('siedler:combat_state') == 'move'" }
          ]
        },
        "move": {
          "animations": ["walk"],
          "transitions": [
            { "attack": "query.property('siedler:combat_state') == 'attack'" },
            { "default": "query.property('siedler:combat_state') != 'move'" }
          ],
          "blend_transition": 0.12
        },
        "attack": {
          "animations": ["attack"],
          "transitions": [
            { "move": "query.property('siedler:combat_state') == 'move' && query.anim_time >= 0.70" },
            { "default": "query.property('siedler:combat_state') != 'attack' || query.anim_time >= 1.15" }
          ],
          "blend_transition": 0.08
        }
      }
    }
  }
}
